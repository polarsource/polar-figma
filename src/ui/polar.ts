import { Polar } from "@polar-sh/sdk";

export const polar = new Polar({
	// We recommend using the sandbox server for development
	// Switch this to "production" (or omit the parameter) when you're ready to go live
	server: "sandbox",
});
